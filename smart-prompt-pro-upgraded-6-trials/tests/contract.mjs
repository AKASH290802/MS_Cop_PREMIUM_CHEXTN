import fs from 'node:fs';import assert from 'node:assert/strict';const root=new URL('../',import.meta.url);const manifest=JSON.parse(fs.readFileSync(new URL('manifest.json',root),'utf8'));const constants=fs.readFileSync(new URL('utils/constants.js',root),'utf8');const content=fs.readFileSync(new URL('content/content.js',root),'utf8');const popup=fs.readFileSync(new URL('popup/popup.js',root),'utf8');assert.equal(manifest.manifest_version,3);assert.match(constants,/FREE_TRIAL_LIMIT:6/);assert.match(content,/smart-prompt-enhance-button/);assert.match(content,/MutationObserver/);assert.match(content,/You have used your 6 free enhancements/);assert.match(popup,/FREE_TRIAL_LIMIT/);console.log('PASS - Smart Prompt 6 trial contract')
